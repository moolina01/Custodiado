/** Small inline spinner shown inside a primary button while its request is in flight (see globals.css's `.flujo-spinner`). */
export default function ButtonSpinner() {
  return <span className="flujo-spinner" aria-hidden="true" />;
}
